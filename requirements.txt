# Core
numpy>=1.26
opencv-contrib-python>=4.9
Pillow>=10.0

# PyTorch — on Mac this installs the MPS-enabled build automatically,
# no special index-url needed (unlike CUDA on Linux/Windows).
# Intel macOS wheels currently top out at torch 2.2.x, which remains
# compatible with Colortina's mc-v2 inference path.
torch>=2.2.2
torchvision>=0.15

# Auto color-hint generation (CLIP zero-shot region labeling)
transformers>=4.40

# Weight download (Google Drive)
gdown>=5.0

# PDF page extraction (for later: batch chapter import)
pymupdf>=1.24

# Pydantic schemas (carried over from ColorComic's models/schemas.py)
pydantic>=2.5


# Phase 2 — desktop editor UI
PySide6>=6.6

# Local ONNX inference — text/bubble protection (comic-text-detector)
# and character-scope segmentation (SkyTNT anime-seg). Both features
# degrade gracefully when the model files are absent.
onnxruntime>=1.20

# Real-ESRGAN upscaling now uses the vendored torch-only RRDBNet in
# vendor/realesrgan_min — the fragile realesrgan/basicsr packages are
# no longer needed. Only the anime6B weights file is required.
